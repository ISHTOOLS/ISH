import fs from'node:fs';import crypto from'node:crypto';const p=JSON.parse(fs.readFileSync('package.json'));const l=JSON.parse(fs.readFileSync('package-lock.json'));const d={...(p.dependencies||{}),...(p.devDependencies||{})};const r=l.packages?.['']||{};const ld={...(r.dependencies||{}),...(r.devDependencies||{})};const mism=Object.keys(d).filter(k=>d[k]!==ld[k]);const out={version:p.version,lockfileVersion:l.lockfileVersion,lockMatches:!mism.length,mismatched:mism,lockSha256:crypto.createHash('sha256').update(fs.readFileSync('package-lock.json')).digest('hex'),npmAudit:'NOT_RUN: registry unavailable in execution environment'};console.log(JSON.stringify(out,null,2));if(mism.length)process.exit(1)
