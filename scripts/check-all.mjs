import fs from 'node:fs';import path from 'node:path';import{spawnSync}from'node:child_process';
const skip=new Set(['node_modules','.git','tmp','release']);const files=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(skip.has(e.name))continue;const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(p.endsWith('.js'))files.push(p)}}walk(process.cwd());const bad=[];for(const f of files){const r=spawnSync(process.execPath,['--check',f],{encoding:'utf8'});if(r.status)bad.push({file:f,error:r.stderr})}if(bad.length){console.error(JSON.stringify(bad,null,2));process.exit(1)}console.log(`JS syntax PASS: ${files.length}/${files.length}`)
